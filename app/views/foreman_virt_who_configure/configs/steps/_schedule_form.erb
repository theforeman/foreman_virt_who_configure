<div class="tab-pane <%= show_config_partial_wizard(step) %>" id="config_schedule">
  <%= text_f f, :interval, :help_block => _("How often to check connected hypervisors for changes (minutes)? Also affects how often a mapping is reported.") %>
</div>
